#include "main.h"
/**
 * swap_int - swaps values of a and b
 * @b: pointer to integer a
 * @b:pointer to b
 *
 * Return:0(success)
 */

void swap_int(int *a, int *b)
{
int *a = &a;
	int *b = &b
		if (a == b && b == a)
{
			_putchar('a');
	_putchar('b');
}
	_putchar('\n');
}

