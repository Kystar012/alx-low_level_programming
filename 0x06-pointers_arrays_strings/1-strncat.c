#include "main.h"
/**
  * _strncat- concatenate two strings
  *@dest:string to cat
  *@src: string to cat
  *@n:string length
  *
  *Return:result(success)
  */
char *_strncat(char *dest, char *src, int n)
{
	char *res;
	int l = 0, m = 0;
while (dest[l] != '\0')
{
	res[m] = dest[l];
	res
l++;
m++;


return (str2);
}
